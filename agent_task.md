# 任务:构建"李大霄视频自动监控分析系统"

你是资深自动化工程师。请在**目标设备(Windows)**上构建并运行一个完全自动化的系统,
监控B站UP主"李大霄"(UID: 2137589551)的视频更新,自动分析并生成每日报告。
**技术栈与平台适配由你自行决策**(Windows下的yt-dlp/ffmpeg/OCR/定时任务等),
本任务书只规定功能、逻辑与输出格式,不预设实现细节。

---

## 系统目标

每天检查李大霄主页是否有新视频;有新视频→自动下载、提取字幕、AI分析,并写入当天的报告文件;
同一天多个视频按时间顺序追加到同一份报告。全程无人值守。

## 功能需求(必须全部实现)

### 1. 更新检测
- **优先使用用户的 bilibili CLI(bilidown)**: https://github.com/menghuanshiguang/bilibili-downloader-cli
  - `bilidown space "2137589551" 1` 获取UP主最新视频(该命令已内置wbi签名+cookies反爬,返回BV号/标题/时长)
- 与状态文件(路径自定,如 `data/last_bvid.txt`)中记录的上次处理BV号对比
- 相同 → 无更新,退出(幂等,可重复运行)
- 不同 → 有新视频,继续流程,完成后更新状态文件
- 支持 `--force` 参数强制重新处理最新视频

### 2. 视频下载
- **优先使用同一bilidown CLI**: `bilidown dl "<BV号>" video <输出目录> mp4 480 1`
  (已内置B站反爬:登录cookies/buvid/UA,裸yt-dlp易被412风控)
- 若该CLI在Windows上运行有问题,可自行适配或选用yt-dlp替代(自行处理cookies)
- 分辨率480P足够OCR使用
- 下载失败重试1次;仍失败则记录错误到报告并跳过

### 3. 字幕提取
- 自行选择Windows可用的硬字幕提取方案(如: ffmpeg抽帧+OCR引擎,
  可选PaddleOCR/RapidVideOCR/Windows OCR等;字幕位置可能不固定,建议先探测)
- 处理要点: 帧存持久目录、OCR失败重试、过滤时间戳/水印噪声、相邻帧去重
- 字幕为空(视频无硬字幕)→ 记录"无字幕,跳过分析",继续其他视频

### 4. AI分析(核心)
用大模型对字幕文本执行分析,规则如下:

**分析规则(李大霄话术解码):**
- 提取核心观点(3-8条,带原文数据:点位/百分比/日期)
- 提取关键数据清单(美债收益率/巴菲特指数/见顶时间表等)
- 暗示提取:"不是推荐"=合规话术下的真实关注点,分"机会暗示/风险警示"两种语境判断;
  每条标注🔴警示/🟢看多/⚪中性/⚠️风险
- 市场定性:反弹/反转/见顶/调整/防御(注意他"没量=不是反转"的核心框架)
- 操作含义:方向/仓位/风险三个层面
- 观点连续性:与历史报告对比,标注"延续/升级/新增/反转"(态度升级如"警惕→高度警惕"重点标出)
- 输出末尾固定:"以上为李大霄个人观点提炼,不构成投资建议"

### 5. 每日报告
- 报告路径: `reports/YYYY-MM-DD.md`(相对工作目录,工作目录自定)
- 报告结构:
  ```
  # 📺 李大霄视频日报 YYYY-MM-DD
  ## 视频清单
  - [时间] 标题 (BV号) 时长
  ## 视频1:《标题》
  (分析结果)
  ## 视频2:《标题》
  (分析结果,当天多个视频追加)
  ```
- 当天已有报告 → 追加(不覆盖);当天无视频 → 不创建报告
- 报告顶部附当日摘要(如"今日观点:警惕升级/新增方向X")

### 6. 状态与幂等
- 状态文件记录最后处理的BV号
- 并发锁防止重复运行
- 维护"已处理BV号列表",即使脚本中断,重跑也不重复分析已处理的视频

## 技术约束
- 目标平台:Windows(技术栈/依赖/定时任务方式由agent自行决策并实施)
- 语言:不限(bash/python/PowerShell等,以Windows可运行为准)
- 现有工具可复用则复用,没有的自行安装,但**不要假设环境里已存在什么**
- 所有中间文件放工作目录下,保持整洁
- 定时运行方式(计划任务/Task Scheduler等)由agent实现并说明

## 交付物
1. 完整可运行的主脚本(实现以上全部逻辑)
2. 手动运行验证:至少完成一次"检测→下载→字幕→分析→报告"全流程
3. 部署说明:如何配置定时运行(Windows任务计划等)

## 验收标准
- [ ] 运行脚本:无新视频时秒退,有新视频时完整处理
- [ ] 重复运行同一视频:不重复分析
- [ ] 同天多个视频:追加到同一份报告
- [ ] 报告格式符合上述结构
- [ ] 无字幕视频:优雅跳过并记录
- [ ] 输出"✅ 今日报告:路径"确认信息
